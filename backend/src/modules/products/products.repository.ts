// SQL модуля товаров. Самый сложный репозиторий в проекте.
//
// Что понадобится:
//   findMany(filters, sort, page, limit) — список с фильтрами и сортировкой
//   countMany(filters)                   — сколько всего, для пагинации
//   findBySlug(slug)                     — карточка товара с характеристиками
//   findSimilar(productId, limit)        — блок «Похожие товары»
//
// Главная сложность: WHERE собирается динамически — фильтров может быть
// один, может быть шесть. Собирай массив условий и массив значений
// параллельно, чтобы номера $1, $2... не разъехались.
