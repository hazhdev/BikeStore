// Бизнес-логика товаров.
//
// Что понадобится:
//   getProductList(query) — разобрать фильтры, сходить в репозиторий,
//                           вернуть товары + информацию о страницах
//   getProductBySlug(slug) — товар или AppError(404)
//   getSimilar(slug)       — похожие товары
//   рассрочка: 6 / 12 / 24 / 60 месяцев — расчёт платежа по цене
//
// Считать рассрочку на бэке, а не на фронте: правила расчёта
// не должны жить в браузере, их меняют без релиза фронта.
