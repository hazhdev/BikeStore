// Схемы валидации входящих данных (JSON Schema для Fastify).
//
// Что описать:
//   params для GET /categories/:slug
//   тело для создания и изменения категории (только для админа)
//
// Обязательно additionalProperties: false — как в auth.schema.ts
