export type NavMenuItem = {
  id: string;
  href: string;
  label: string;
};

export type IconMenuItem = {
  id: string;
  href: string;
  label: string;
  icon: string;
};

export type typefooterLinks = {
  id: string;
  href: string;
  label: string;
};
